api:
  listen: :1984

rtsp:
  listen: :8554

log:
  level: info

# Стримы НЕ прописываются здесь статически — go2rtc моментально начинает
# тянуть источник и буферизировать в памяти даже без клиентов.
# Вместо этого бэкенд регистрирует стрим через PUT /api/streams только
# когда пользователь открывает камеру (on-demand через ensureStream).
# streams: {}  ← не нужно
streams:
  test_vlc:
    - http://192.168.10.247:8080/live
  device_16_ch1_main:
    - rtsp://192.168.10.247:554/live
